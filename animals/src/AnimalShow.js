function AnimalShow() {

	return (
		<div>
			{/* Variation 04 of Event Handler */}
			<button onClick={() => console.log("Button was clicked!")}
			>Add Animal</button>
		</div>
	);
}

export default AnimalShow;